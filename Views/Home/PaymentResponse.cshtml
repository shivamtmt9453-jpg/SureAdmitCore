@model SureAdmitCore.Models.CourseCheckoutModel

@{
    ViewData["Title"] = "Payment Response";
    Layout = null;
}
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="~/assets/css/cartpage.css" rel="stylesheet" />
    <link href="~/assets/css/checkout.css" rel="stylesheet" />
</head>
<div class="container mt-5 paymentresponse">
    <h2>Payment <span>Status</span></h2>

    @if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <div class="alert @(ViewBag.Message.Contains("Successful") ? "alert-success" : "alert-danger")">
            @ViewBag.Message
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.PaymentId))
        {
            <p>Payment Reference: <strong>@ViewBag.PaymentId</strong></p>
        }
    }
    else
    {
        <div class="alert alert-warning">
            Payment information not found.
        </div>
    }
    <div class="bactohome">
        <a href="/home/Scholaria" class="">Back to Home</a>
    </div>
</div>